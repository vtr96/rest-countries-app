# Database
DB_HOST=localhost
DB_PORT=27017
DB_NAME=restcountries
DB_USER=your_db_user
DB_PASS=your_db_password

# Or full connection URI (MongoDB example)
MONGO_URI=mongodb://localhost:27017/restcountries

# Auth / JWT
JWT_SECRET=supersecretkey123
JWT_EXPIRES_IN=1h

# Server
PORT=3000